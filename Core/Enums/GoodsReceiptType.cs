namespace Core.Enums;

public enum GoodsReceiptType {
    All = 0,              // Auto confirm all
    SpecificOrders = 1,   // Specific orders
    SpecificReceipts = 2, // Specific receipts
    SpecificTransfers = 3  // Specific transfer
}